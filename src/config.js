const serverIP = 'localhost:8080'

module.exports = {
    registrationURL: `http://${serverIP}/registration`,
    createTeamURL: `http://${serverIP}/create-team`,
    positionsURL: `http://${serverIP}/positions`
}